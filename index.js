import inquirer from 'inquirer';

inquirer
  .prompt([
    {
    // 질문들을 정의하는 배열
    
    }
  ])
  .then((answers) => {
    // 사용자가 입력한 답변들을 처리하는 코드
    
  })
  .catch((error)=> {
    // 오류 처리 코드
  });